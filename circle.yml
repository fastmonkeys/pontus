machine:
  python:
    version: 2.7.6

dependencies:
  override:
    - pip install -e  ".[test]"

test:
  override:
    - py.test:
        parallel: true
        files:
          - tests/**/test_*.py
